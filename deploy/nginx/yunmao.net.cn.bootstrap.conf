server {
    listen 80;
    listen [::]:80;
    server_name yunmao.net.cn;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'yunmao bootstrap ready';
        add_header Content-Type text/plain;
    }
}
